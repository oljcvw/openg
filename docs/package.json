{
	"name": "open-grind-docs",
	"private": true,
	"license": "MIT",
	"type": "module",
	"module": "index.ts",
	"scripts": {
		"audit:coverage": "bun run scripts/audit-coverage.ts",
		"audit:links": "bun run scripts/audit-links.ts",
		"audit:openapi": "bun run scripts/audit-openapi.ts",
		"audit:queries": "bun run scripts/audit-queries.ts",
		"build": "bun run generate && bun run build:site",
		"build:site": "vitepress build",
		"check": "bun run format:check && bun run lint:markdown && bun run validate && bun run generate && bun run audit:openapi && bun run audit:coverage && bun run audit:queries && bun run audit:links && bun run build:site && git diff --exit-code -- lib/index.ts",
		"cleanup": "bun run scripts/cleanup-openapi.ts",
		"deploy": "bun run build && bun gh-pages -d .vitepress/dist",
		"generate": "bun run scripts/generate-docs.ts && prettier --write lib/index.ts",
		"lint:markdown": "markdownlint-cli2",
		"lint:markdown:staged": "markdownlint-cli2 --no-globs",
		"format:check": "prettier --check .",
		"preview": "vitepress preview",
		"start": "bun run generate && vitepress dev",
		"validate": "bun run scripts/validate-openapi.ts"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"gh-pages": "^6.3.0",
		"markdownlint-cli2": "^0.23.2",
		"prettier": "^3.8.4",
		"vitepress": "^2.0.0-alpha.17"
	},
	"peerDependencies": {
		"typescript": "^6"
	}
}
